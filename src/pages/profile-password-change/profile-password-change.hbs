<div class='page'>
  <div class="page-wrapper">
    {{> BackLink}}
    <main class="profile">
      <form class="profile__form">
        {{{Avatar}}}
        <h4>{{name}}</h4>
        <section>
          {{#each Fields}}
            {{{this}}}
          {{/each}}
        </section>
        {{{Button}}}
      </form>
    </main>
  </div>
  {{{DevModeNav}}}
</div>
