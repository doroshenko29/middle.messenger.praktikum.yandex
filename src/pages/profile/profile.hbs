<div class="page-wrapper prifile-page">
  {{> BackLink}}
  <main class="profile">
    <form class="profile__form">
      {{{avatar}}}
      <h4>{{name}}</h4>
      {{{fields}}}
    </form>
    <section>
      {{{formLinks}}}
    </section>
  </main>
</div>
{{{devModeNav}}}
