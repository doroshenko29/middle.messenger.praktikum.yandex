{
  "name": "middle.messenger.praktikum.yandex",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite build && vite --port 3000 --open",
    "dev": "vite --port 3000 --open --mode=development",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass-embedded": "^1.78.0",
    "vite": "^5.4.1",
    "vite-plugin-handlebars": "^2.0.0"
  }
}
